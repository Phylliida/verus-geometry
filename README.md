# verus-geometry

Verified geometric predicates (orientation, collinearity, coplanarity) parameterized over any `Ring`, built on [verus-linalg](https://github.com/Phylliida/verus-linalg) and [verus-algebra](https://github.com/Phylliida/verus-algebra).

**29 verified items**, 0 errors, 0 `assume`/`admit`.

## Verification

```bash
VERUS_ROOT=/path/to/verus ./scripts/check.sh --require-verus --forbid-trusted-escapes --min-verified 29
```
